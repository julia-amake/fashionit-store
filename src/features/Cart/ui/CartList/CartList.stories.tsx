import type { Meta } from '@storybook/react';
import { CartList } from './CartList';

const meta: Meta<typeof CartList> = {
  title: 'shared/ProductCartList',
  component: CartList,
  args: {},
};

export default meta;

export const Default = {};
