import type { Meta } from '@storybook/react';
import { CartPage } from './CartPage';

const meta: Meta<typeof CartPage> = {
  title: 'shared/CartPage',
  component: CartPage,
  args: {},
};

export default meta;

export const Default = {};
